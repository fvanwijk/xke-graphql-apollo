schema: schema.gql
generates:
  types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
    config:
      maybeValue: T | undefined
      mappers:
        Book: ./models#BookModel
watch: true
hooks:
  afterOneFileWrite:
    - npx prettier --write
